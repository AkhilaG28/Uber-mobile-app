import React, { FC } from "react";
import { Text, View, SafeAreaView } from "react-native";

const HomeScreen: FC = () => {
  return (
    <SafeAreaView>
      <Text>Home screen of Uber</Text>
    </SafeAreaView>
  );
};

export default HomeScreen;
